# GiftBrain AI (v2 – Feedback Ready)

This version supports explicit feedback collection for pre-launch training.

## Run
```bash
npm install
node api/index.js
```

## Endpoints
POST /recommend  
POST /feedback
